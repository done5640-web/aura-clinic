// Module-level set — persists across navigation within the same browser session.
// Lead IDs added here are sorted to the bottom of the leads list.
export const recentStatusChanges = new Set<string>();
